<h3>UploadFile Component</h3>

<InputFile OnChange="@uploadAFile"></InputFile>

@code {
    public void uploadAFile(InputFileChangeEventArgs eventArgs)
    {
        StreamReader reader = new StreamReader(eventArgs);
    }
}
